pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
	stage('run') {
            steps {
                sh 'ls'
            }
        }
    }
}
